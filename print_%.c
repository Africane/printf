#include "main.h"

/**
 * print_37 - prints % character
 * @c: the character to print
 *
 * Return: 1 on success, -1 on error and err no is set appropriately
 */

int _putchar(char c)
{
        return (write(1, &c, 1));
}

